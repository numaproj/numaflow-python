[project]
name = "reduce-counter"
version = "0.2.4"
description = ""
requires-python = ">=3.13"
dependencies = [
    "pynumaflow",
    "aiorun>=2025.1.1",
    "aiohttp>=3.8.4",
]

[tool.uv.sources]
pynumaflow = { path = "../../../" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
