[project]
name = "map-flatmap-stream"
version = "0.2.4"
description = ""
requires-python = ">=3.13"
dependencies = [
    "pynumaflow",
]

[tool.uv.sources]
pynumaflow = { path = "../../../" }

[tool.hatch.build.targets.wheel]
packages = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
